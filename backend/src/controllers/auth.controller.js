export const handleSignup = (request, response) => {
    response.send("sigup route")
};  

export const handleLogin = (request, response) => {
    response.send("login route")
};

export const handleLogout = (request, response) => {
    response.send("logout route")
}; 